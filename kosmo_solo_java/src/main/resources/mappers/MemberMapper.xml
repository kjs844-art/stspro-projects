<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joe.java.member.MemberMapper">

    <!-- C: Insert a new member -->
    <insert id="insertMember" parameterType="MemberDTO" useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO member (user_id, password, name, email, phone, role)
        VALUES (#{userId}, #{password}, #{name}, #{email}, #{phone}, #{role})
    </insert>

    <!-- R: Select all members -->
    <select id="selectMemberList" resultType="MemberDTO">
        SELECT * FROM member ORDER BY member_id DESC
    </select>

    <!-- R: Select one member by primary key -->
    <select id="selectMemberById" parameterType="Long" resultType="MemberDTO">
        SELECT * FROM member WHERE member_id = #{memberId}
    </select>

    <!-- R: Select one member by login userId -->
    <select id="selectMemberByUserId" parameterType="String" resultType="MemberDTO">
        SELECT * FROM member WHERE user_id = #{userId}
    </select>

    <!-- U: Update member details (except password & role here) -->
    <update id="updateMember" parameterType="MemberDTO">
        UPDATE member
        SET name = #{name}, email = #{email}, phone = #{phone}
        WHERE member_id = #{memberId}
    </update>

    <!-- D: Delete a member -->
    <delete id="deleteMember" parameterType="Long">
        DELETE FROM member WHERE member_id = #{memberId}
    </delete>

</mapper>
